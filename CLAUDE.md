# 프로젝트: COMPASS (사회적 경로 결정 논문 + 구현)

## 역할
ROS2 서비스 로봇의 통과 결정 시간 일관성 연구를 논문화·구현한다.
**진실 원천:** `paper/HANDOFF.md` §4(확정 설계)와 `paper/paper_draft.md`(현재 v0.5). §4 확정 설계는 재논의·변경하지 않는다.

## 규약
- 문서는 한국어 **합쇼체**. 기술 용어는 원문 + 백틱 유지(예: `class`, `Nav2`, `TTC`, `J_social`, `e^fwd`, `e^rev`, `ρ̄`, `θ*`).
- 다섯 작업 원칙: 기본적 회의주의 · 결정의 외부화 · 경계 선언 우선 · 가역적 점진 변경 · 완료 정의.
- 수식은 백틱/코드블록으로 표기(최종 LaTeX 변환 시 정리).
- 그림은 `paper/figures/`에 SVG 또는 tikz로, 본문 §4 절마다 1:1 대응.
- **연구 정직성(절대):** 측정 수치를 날조하지 않는다. 시뮬레이터·실로봇 미구축이므로 R1~R4·O1의 측정값은 `[TBD]`로 유지한다. 가짜 숫자·p-value·지연(ms)·성공률 금지.
- 서명 금지: 커밋·문서에 "Generated with Claude Code" / "Co-Authored-By: Claude" 류 서명을 넣지 않는다.

## 현재 상태
- 논문 v0.5 — **텍스트 완료**. 4라운드 모사 심사(저자 + 심사 A·B·C + 편집자) 수렴, 편집자 `textComplete=true`.
- 잔여 차단 요인 = 실험(R1~R4·O1) 측정 + 그림 4종 + `src/` 구현. 모두 시뮬레이터 구축이 선행 조건.

## 다음 작업 (게이팅 순서)
1. 시뮬레이터(Gazebo/Isaac + PedSim 류) 구축 — 시뮬–비용함수 순환성(심사 B2) 주의.
2. `src/`에 `Nav2` 플러그인 구현 (논문 §4.5 통합 알고리즘 1:1).
3. 실험으로 `paper_draft.md`의 `[TBD]` 채우기. ablation 표를 가설→측정값으로 교체, 비모수 검정·효과 크기·시드/시행 수 보고.

## 재사용 가능한 개정 루프
3인 심사(A 이론 / B 시스템 / C HRI) + 편집자 종합 패턴은 개정본 회귀 검수에 그대로 재사용 가능하다. 워크플로 스크립트 원형: 세션 tmp의 `paper_revision_loop.js`(저자 1 → 심사 3 병렬 → 편집자, 수렴까지 반복). 측정 데이터 날조 금지 가드와 experiment-gated 판별이 내장되어 있다.

## 완료 정의
각 심사 항목(R/O)은 (a) 본문 수정 위치 명시 (b) 심사 우려와의 대응 명기 (c) diff 최소·가역. 측정이 필요한 항목은 `[TBD]`로 두되 무엇을·어떻게 측정할지 본문에 기술.
